// Code generated by cmd/codegen from services/pubsub/spec/aws-sns.smithy.json.
// Upstream commit: 2517fe9ffa52ed4507b13ccc57efa111b2008750.
// DO NOT EDIT.

package gen

import (
	"context"
	"encoding/xml"
	"io"
	"net/http"
	"strconv"
	"strings"
	"time"

	"github.com/e6qu/shimanism/internal/awsquery"
)

// Keep imports referenced even when an emitted operation set doesn't
// exercise every facility.
var (
	_ context.Context
	_ http.Handler
	_ time.Time
	_ xml.Name
	_ io.Reader
	_ = strconv.ParseInt
	_ = strings.HasPrefix
	_ awsquery.Decoder
)

// TagList is a generated Smithy list. The Member field is
// XML-tagged with the element name from the spec's @xmlName trait
// (defaulting to the target shape's short name when absent), so
// SDK clients see the per-list element name they expect — e.g.
// `<DBInstance>` for RDS DBInstanceList, `<member>` for SNS
// TopicsList.
type TagList struct {
	Member []Tag `xml:"member"`
}

// TopicsList is a generated Smithy list. The Member field is
// XML-tagged with the element name from the spec's @xmlName trait
// (defaulting to the target shape's short name when absent), so
// SDK clients see the per-list element name they expect — e.g.
// `<DBInstance>` for RDS DBInstanceList, `<member>` for SNS
// TopicsList.
type TopicsList struct {
	Member []Topic `xml:"member"`
}

// SubscriptionsList is a generated Smithy list. The Member field is
// XML-tagged with the element name from the spec's @xmlName trait
// (defaulting to the target shape's short name when absent), so
// SDK clients see the per-list element name they expect — e.g.
// `<DBInstance>` for RDS DBInstanceList, `<member>` for SNS
// TopicsList.
type SubscriptionsList struct {
	Member []Subscription `xml:"member"`
}

// TopicAttributesMap is a generated Smithy map.
type TopicAttributesMap map[string]string

// SubscriptionAttributesMap is a generated Smithy map.
type SubscriptionAttributesMap map[string]string

// MessageAttributeMap is a generated Smithy map.
type MessageAttributeMap map[string]*MessageAttributeValue

// Tag is a generated Smithy structure.
type Tag struct {
	Key   string `xml:"Key,omitempty"`
	Value string `xml:"Value,omitempty"`
}

// CreateTopicInput is a generated Smithy structure.
type CreateTopicInput struct {
	Attributes           TopicAttributesMap `xml:"Attributes,omitempty"`
	DataProtectionPolicy *string            `xml:"DataProtectionPolicy,omitempty"`
	Name                 string             `xml:"Name,omitempty"`
	Tags                 TagList            `xml:"Tags,omitempty"`
}

// CreateTopicResponse is a generated Smithy structure.
type CreateTopicResponse struct {
	TopicArn *string `xml:"TopicArn,omitempty"`
}

// AuthorizationErrorException is a generated Smithy structure. It is an error response (HTTP 403).
type AuthorizationErrorException struct {
	Message *string `xml:"Message,omitempty"`
}

// ConcurrentAccessException is a generated Smithy structure. It is an error response (HTTP 400).
type ConcurrentAccessException struct {
	Message *string `xml:"Message,omitempty"`
}

// InternalErrorException is a generated Smithy structure. It is an error response (HTTP 500).
type InternalErrorException struct {
	Message *string `xml:"Message,omitempty"`
}

// InvalidParameterException is a generated Smithy structure. It is an error response (HTTP 400).
type InvalidParameterException struct {
	Message *string `xml:"Message,omitempty"`
}

// InvalidSecurityException is a generated Smithy structure. It is an error response (HTTP 403).
type InvalidSecurityException struct {
	Message *string `xml:"Message,omitempty"`
}

// StaleTagException is a generated Smithy structure. It is an error response (HTTP 400).
type StaleTagException struct {
	Message *string `xml:"Message,omitempty"`
}

// TagLimitExceededException is a generated Smithy structure. It is an error response (HTTP 400).
type TagLimitExceededException struct {
	Message *string `xml:"Message,omitempty"`
}

// TagPolicyException is a generated Smithy structure. It is an error response (HTTP 400).
type TagPolicyException struct {
	Message *string `xml:"Message,omitempty"`
}

// TopicLimitExceededException is a generated Smithy structure. It is an error response (HTTP 403).
type TopicLimitExceededException struct {
	Message *string `xml:"Message,omitempty"`
}

// DeleteTopicInput is a generated Smithy structure.
type DeleteTopicInput struct {
	TopicArn string `xml:"TopicArn,omitempty"`
}

// InvalidStateException is a generated Smithy structure. It is an error response (HTTP 400).
type InvalidStateException struct {
	Message *string `xml:"Message,omitempty"`
}

// NotFoundException is a generated Smithy structure. It is an error response (HTTP 404).
type NotFoundException struct {
	Message *string `xml:"Message,omitempty"`
}

// ListTopicsInput is a generated Smithy structure.
type ListTopicsInput struct {
	NextToken *string `xml:"NextToken,omitempty"`
}

// Topic is a generated Smithy structure.
type Topic struct {
	TopicArn *string `xml:"TopicArn,omitempty"`
}

// ListTopicsResponse is a generated Smithy structure.
type ListTopicsResponse struct {
	NextToken *string    `xml:"NextToken,omitempty"`
	Topics    TopicsList `xml:"Topics,omitempty"`
}

// SubscribeInput is a generated Smithy structure.
type SubscribeInput struct {
	Attributes            SubscriptionAttributesMap `xml:"Attributes,omitempty"`
	Endpoint              *string                   `xml:"Endpoint,omitempty"`
	Protocol              string                    `xml:"Protocol,omitempty"`
	ReturnSubscriptionArn *bool                     `xml:"ReturnSubscriptionArn,omitempty"`
	TopicArn              string                    `xml:"TopicArn,omitempty"`
}

// SubscribeResponse is a generated Smithy structure.
type SubscribeResponse struct {
	SubscriptionArn *string `xml:"SubscriptionArn,omitempty"`
}

// FilterPolicyLimitExceededException is a generated Smithy structure. It is an error response (HTTP 403).
type FilterPolicyLimitExceededException struct {
	Message *string `xml:"Message,omitempty"`
}

// ReplayLimitExceededException is a generated Smithy structure. It is an error response (HTTP 403).
type ReplayLimitExceededException struct {
	Message *string `xml:"Message,omitempty"`
}

// SubscriptionLimitExceededException is a generated Smithy structure. It is an error response (HTTP 403).
type SubscriptionLimitExceededException struct {
	Message *string `xml:"Message,omitempty"`
}

// UnsubscribeInput is a generated Smithy structure.
type UnsubscribeInput struct {
	SubscriptionArn string `xml:"SubscriptionArn,omitempty"`
}

// ListSubscriptionsInput is a generated Smithy structure.
type ListSubscriptionsInput struct {
	NextToken *string `xml:"NextToken,omitempty"`
}

// Subscription is a generated Smithy structure.
type Subscription struct {
	Endpoint        *string `xml:"Endpoint,omitempty"`
	Owner           *string `xml:"Owner,omitempty"`
	Protocol        *string `xml:"Protocol,omitempty"`
	SubscriptionArn *string `xml:"SubscriptionArn,omitempty"`
	TopicArn        *string `xml:"TopicArn,omitempty"`
}

// ListSubscriptionsResponse is a generated Smithy structure.
type ListSubscriptionsResponse struct {
	NextToken     *string           `xml:"NextToken,omitempty"`
	Subscriptions SubscriptionsList `xml:"Subscriptions,omitempty"`
}

// ListSubscriptionsByTopicInput is a generated Smithy structure.
type ListSubscriptionsByTopicInput struct {
	NextToken *string `xml:"NextToken,omitempty"`
	TopicArn  string  `xml:"TopicArn,omitempty"`
}

// ListSubscriptionsByTopicResponse is a generated Smithy structure.
type ListSubscriptionsByTopicResponse struct {
	NextToken     *string           `xml:"NextToken,omitempty"`
	Subscriptions SubscriptionsList `xml:"Subscriptions,omitempty"`
}

// MessageAttributeValue is a generated Smithy structure.
type MessageAttributeValue struct {
	BinaryValue []byte  `xml:"BinaryValue,omitempty"`
	DataType    string  `xml:"DataType,omitempty"`
	StringValue *string `xml:"StringValue,omitempty"`
}

// PublishInput is a generated Smithy structure.
type PublishInput struct {
	Message                string              `xml:"Message,omitempty"`
	MessageAttributes      MessageAttributeMap `xml:"MessageAttributes,omitempty"`
	MessageDeduplicationId *string             `xml:"MessageDeduplicationId,omitempty"`
	MessageGroupId         *string             `xml:"MessageGroupId,omitempty"`
	MessageStructure       *string             `xml:"MessageStructure,omitempty"`
	PhoneNumber            *string             `xml:"PhoneNumber,omitempty"`
	Subject                *string             `xml:"Subject,omitempty"`
	TargetArn              *string             `xml:"TargetArn,omitempty"`
	TopicArn               *string             `xml:"TopicArn,omitempty"`
}

// PublishResponse is a generated Smithy structure.
type PublishResponse struct {
	MessageId      *string `xml:"MessageId,omitempty"`
	SequenceNumber *string `xml:"SequenceNumber,omitempty"`
}

// EndpointDisabledException is a generated Smithy structure. It is an error response (HTTP 400).
type EndpointDisabledException struct {
	Message *string `xml:"Message,omitempty"`
}

// InvalidParameterValueException is a generated Smithy structure. It is an error response (HTTP 400).
type InvalidParameterValueException struct {
	Message *string `xml:"Message,omitempty"`
}

// KMSAccessDeniedException is a generated Smithy structure. It is an error response (HTTP 400).
type KMSAccessDeniedException struct {
	Message *string `xml:"Message,omitempty"`
}

// KMSDisabledException is a generated Smithy structure. It is an error response (HTTP 400).
type KMSDisabledException struct {
	Message *string `xml:"Message,omitempty"`
}

// KMSInvalidStateException is a generated Smithy structure. It is an error response (HTTP 400).
type KMSInvalidStateException struct {
	Message *string `xml:"Message,omitempty"`
}

// KMSNotFoundException is a generated Smithy structure. It is an error response (HTTP 400).
type KMSNotFoundException struct {
	Message *string `xml:"Message,omitempty"`
}

// KMSOptInRequired is a generated Smithy structure. It is an error response (HTTP 403).
type KMSOptInRequired struct {
	Message *string `xml:"Message,omitempty"`
}

// KMSThrottlingException is a generated Smithy structure. It is an error response (HTTP 400).
type KMSThrottlingException struct {
	Message *string `xml:"Message,omitempty"`
}

// PlatformApplicationDisabledException is a generated Smithy structure. It is an error response (HTTP 400).
type PlatformApplicationDisabledException struct {
	Message *string `xml:"Message,omitempty"`
}

// ValidationException is a generated Smithy structure. It is an error response (HTTP 400).
type ValidationException struct {
	Message string `xml:"Message,omitempty"`
}

// GetTopicAttributesInput is a generated Smithy structure.
type GetTopicAttributesInput struct {
	TopicArn string `xml:"TopicArn,omitempty"`
}

// GetTopicAttributesResponse is a generated Smithy structure.
type GetTopicAttributesResponse struct {
	Attributes TopicAttributesMap `xml:"Attributes,omitempty"`
}

// SetTopicAttributesInput is a generated Smithy structure.
type SetTopicAttributesInput struct {
	AttributeName  string  `xml:"AttributeName,omitempty"`
	AttributeValue *string `xml:"AttributeValue,omitempty"`
	TopicArn       string  `xml:"TopicArn,omitempty"`
}

// ListTagsForResourceRequest is a generated Smithy structure.
type ListTagsForResourceRequest struct {
	ResourceArn string `xml:"ResourceArn,omitempty"`
}

// ListTagsForResourceResponse is a generated Smithy structure.
type ListTagsForResourceResponse struct {
	Tags TagList `xml:"Tags,omitempty"`
}

// ResourceNotFoundException is a generated Smithy structure. It is an error response (HTTP 404).
type ResourceNotFoundException struct {
	Message *string `xml:"Message,omitempty"`
}

// SNSBackend is the union of every per-operation
// backend interface emitted from the spec.
type SNSBackend interface {
	CreateTopicBackend
	DeleteTopicBackend
	ListTopicsBackend
	SubscribeBackend
	UnsubscribeBackend
	ListSubscriptionsBackend
	ListSubscriptionsByTopicBackend
	PublishBackend
	GetTopicAttributesBackend
	SetTopicAttributesBackend
	ListTagsForResourceBackend
}

// RegisterSNSRoutes mounts every shimmed operation
// handler onto an awsquery.Router and returns it. The protocol
// routes by the `Action` form parameter; no per-route disambiguation
// is needed.
func RegisterSNSRoutes(b SNSBackend) *awsquery.Router {
	rt := awsquery.NewRouter("AmazonSimpleNotificationService")
	rt.Register("CreateTopic", CreateTopicHandler(b))
	rt.Register("DeleteTopic", DeleteTopicHandler(b))
	rt.Register("ListTopics", ListTopicsHandler(b))
	rt.Register("Subscribe", SubscribeHandler(b))
	rt.Register("Unsubscribe", UnsubscribeHandler(b))
	rt.Register("ListSubscriptions", ListSubscriptionsHandler(b))
	rt.Register("ListSubscriptionsByTopic", ListSubscriptionsByTopicHandler(b))
	rt.Register("Publish", PublishHandler(b))
	rt.Register("GetTopicAttributes", GetTopicAttributesHandler(b))
	rt.Register("SetTopicAttributes", SetTopicAttributesHandler(b))
	rt.Register("ListTagsForResource", ListTagsForResourceHandler(b))
	return rt
}

// CreateTopicBackend serves the CreateTopic operation.
type CreateTopicBackend interface {
	CreateTopic(ctx context.Context, in *CreateTopicInput) (*CreateTopicResponse, error)
}

// CreateTopicHandler decodes a CreateTopic request, dispatches
// to the backend, and encodes the response per awsQuery semantics.
// Form-encoded request bodies are decoded field-by-field for
// scalars, map<string,string>, and list<string> shapes. Complex
// collections (map<string,struct> like SNS MessageAttributes) are
// available to the backend via awsquery.FormFromContext(ctx).
func CreateTopicHandler(b CreateTopicBackend) http.Handler {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := awsquery.WithForm(r.Context(), r.Form)
		in := &CreateTopicInput{}
		_ = in
		// Decode top-level scalar + map<string,string> + list<string>
		// form fields. AWS awsQuery serialises these as flat
		// name=value pairs with `.entry.N.{key,value}` (maps) and
		// `.member.N` (lists) conventions.
		// map<string,string> via `Attributes.entry.N.{key,value}`.
		{
			seen := map[string]bool{}
			for k := range r.Form {
				prefix := "Attributes.entry."
				if !strings.HasPrefix(k, prefix) {
					continue
				}
				rest := strings.TrimPrefix(k, prefix)
				dot := strings.IndexByte(rest, '.')
				if dot < 0 {
					continue
				}
				idx := rest[:dot]
				if seen[idx] {
					continue
				}
				seen[idx] = true
				kv := r.Form.Get(prefix + idx + ".key")
				vv := r.Form.Get(prefix + idx + ".value")
				if kv == "" {
					continue
				}
				if in.Attributes == nil {
					in.Attributes = TopicAttributesMap{}
				}
				in.Attributes[kv] = vv
			}
		}
		if v := r.Form.Get("DataProtectionPolicy"); v != "" {
			s := v
			in.DataProtectionPolicy = &s
		}
		in.Name = r.Form.Get("Name")

		out, err := b.CreateTopic(ctx, in)
		if err != nil {
			awsquery.WriteBackendError(w, err)
			return
		}
		awsquery.WriteResult(w, "CreateTopic", out)
	})
}

// DeleteTopicBackend serves the DeleteTopic operation.
type DeleteTopicBackend interface {
	DeleteTopic(ctx context.Context, in *DeleteTopicInput) (struct{}, error)
}

// DeleteTopicHandler decodes a DeleteTopic request, dispatches
// to the backend, and encodes the response per awsQuery semantics.
// Form-encoded request bodies are decoded field-by-field for
// scalars, map<string,string>, and list<string> shapes. Complex
// collections (map<string,struct> like SNS MessageAttributes) are
// available to the backend via awsquery.FormFromContext(ctx).
func DeleteTopicHandler(b DeleteTopicBackend) http.Handler {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := awsquery.WithForm(r.Context(), r.Form)
		in := &DeleteTopicInput{}
		_ = in
		// Decode top-level scalar + map<string,string> + list<string>
		// form fields. AWS awsQuery serialises these as flat
		// name=value pairs with `.entry.N.{key,value}` (maps) and
		// `.member.N` (lists) conventions.
		in.TopicArn = r.Form.Get("TopicArn")

		if _, err := b.DeleteTopic(ctx, in); err != nil {
			awsquery.WriteBackendError(w, err)
			return
		}
		awsquery.WriteResult(w, "DeleteTopic", nil)
	})
}

// ListTopicsBackend serves the ListTopics operation.
type ListTopicsBackend interface {
	ListTopics(ctx context.Context, in *ListTopicsInput) (*ListTopicsResponse, error)
}

// ListTopicsHandler decodes a ListTopics request, dispatches
// to the backend, and encodes the response per awsQuery semantics.
// Form-encoded request bodies are decoded field-by-field for
// scalars, map<string,string>, and list<string> shapes. Complex
// collections (map<string,struct> like SNS MessageAttributes) are
// available to the backend via awsquery.FormFromContext(ctx).
func ListTopicsHandler(b ListTopicsBackend) http.Handler {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := awsquery.WithForm(r.Context(), r.Form)
		in := &ListTopicsInput{}
		_ = in
		// Decode top-level scalar + map<string,string> + list<string>
		// form fields. AWS awsQuery serialises these as flat
		// name=value pairs with `.entry.N.{key,value}` (maps) and
		// `.member.N` (lists) conventions.
		if v := r.Form.Get("NextToken"); v != "" {
			s := v
			in.NextToken = &s
		}

		out, err := b.ListTopics(ctx, in)
		if err != nil {
			awsquery.WriteBackendError(w, err)
			return
		}
		awsquery.WriteResult(w, "ListTopics", out)
	})
}

// SubscribeBackend serves the Subscribe operation.
type SubscribeBackend interface {
	Subscribe(ctx context.Context, in *SubscribeInput) (*SubscribeResponse, error)
}

// SubscribeHandler decodes a Subscribe request, dispatches
// to the backend, and encodes the response per awsQuery semantics.
// Form-encoded request bodies are decoded field-by-field for
// scalars, map<string,string>, and list<string> shapes. Complex
// collections (map<string,struct> like SNS MessageAttributes) are
// available to the backend via awsquery.FormFromContext(ctx).
func SubscribeHandler(b SubscribeBackend) http.Handler {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := awsquery.WithForm(r.Context(), r.Form)
		in := &SubscribeInput{}
		_ = in
		// Decode top-level scalar + map<string,string> + list<string>
		// form fields. AWS awsQuery serialises these as flat
		// name=value pairs with `.entry.N.{key,value}` (maps) and
		// `.member.N` (lists) conventions.
		// map<string,string> via `Attributes.entry.N.{key,value}`.
		{
			seen := map[string]bool{}
			for k := range r.Form {
				prefix := "Attributes.entry."
				if !strings.HasPrefix(k, prefix) {
					continue
				}
				rest := strings.TrimPrefix(k, prefix)
				dot := strings.IndexByte(rest, '.')
				if dot < 0 {
					continue
				}
				idx := rest[:dot]
				if seen[idx] {
					continue
				}
				seen[idx] = true
				kv := r.Form.Get(prefix + idx + ".key")
				vv := r.Form.Get(prefix + idx + ".value")
				if kv == "" {
					continue
				}
				if in.Attributes == nil {
					in.Attributes = SubscriptionAttributesMap{}
				}
				in.Attributes[kv] = vv
			}
		}
		if v := r.Form.Get("Endpoint"); v != "" {
			s := v
			in.Endpoint = &s
		}
		in.Protocol = r.Form.Get("Protocol")
		if v := r.Form.Get("ReturnSubscriptionArn"); v != "" {
			if x, err := strconv.ParseBool(v); err == nil {
				in.ReturnSubscriptionArn = &x
			}
		}
		in.TopicArn = r.Form.Get("TopicArn")

		out, err := b.Subscribe(ctx, in)
		if err != nil {
			awsquery.WriteBackendError(w, err)
			return
		}
		awsquery.WriteResult(w, "Subscribe", out)
	})
}

// UnsubscribeBackend serves the Unsubscribe operation.
type UnsubscribeBackend interface {
	Unsubscribe(ctx context.Context, in *UnsubscribeInput) (struct{}, error)
}

// UnsubscribeHandler decodes a Unsubscribe request, dispatches
// to the backend, and encodes the response per awsQuery semantics.
// Form-encoded request bodies are decoded field-by-field for
// scalars, map<string,string>, and list<string> shapes. Complex
// collections (map<string,struct> like SNS MessageAttributes) are
// available to the backend via awsquery.FormFromContext(ctx).
func UnsubscribeHandler(b UnsubscribeBackend) http.Handler {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := awsquery.WithForm(r.Context(), r.Form)
		in := &UnsubscribeInput{}
		_ = in
		// Decode top-level scalar + map<string,string> + list<string>
		// form fields. AWS awsQuery serialises these as flat
		// name=value pairs with `.entry.N.{key,value}` (maps) and
		// `.member.N` (lists) conventions.
		in.SubscriptionArn = r.Form.Get("SubscriptionArn")

		if _, err := b.Unsubscribe(ctx, in); err != nil {
			awsquery.WriteBackendError(w, err)
			return
		}
		awsquery.WriteResult(w, "Unsubscribe", nil)
	})
}

// ListSubscriptionsBackend serves the ListSubscriptions operation.
type ListSubscriptionsBackend interface {
	ListSubscriptions(ctx context.Context, in *ListSubscriptionsInput) (*ListSubscriptionsResponse, error)
}

// ListSubscriptionsHandler decodes a ListSubscriptions request, dispatches
// to the backend, and encodes the response per awsQuery semantics.
// Form-encoded request bodies are decoded field-by-field for
// scalars, map<string,string>, and list<string> shapes. Complex
// collections (map<string,struct> like SNS MessageAttributes) are
// available to the backend via awsquery.FormFromContext(ctx).
func ListSubscriptionsHandler(b ListSubscriptionsBackend) http.Handler {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := awsquery.WithForm(r.Context(), r.Form)
		in := &ListSubscriptionsInput{}
		_ = in
		// Decode top-level scalar + map<string,string> + list<string>
		// form fields. AWS awsQuery serialises these as flat
		// name=value pairs with `.entry.N.{key,value}` (maps) and
		// `.member.N` (lists) conventions.
		if v := r.Form.Get("NextToken"); v != "" {
			s := v
			in.NextToken = &s
		}

		out, err := b.ListSubscriptions(ctx, in)
		if err != nil {
			awsquery.WriteBackendError(w, err)
			return
		}
		awsquery.WriteResult(w, "ListSubscriptions", out)
	})
}

// ListSubscriptionsByTopicBackend serves the ListSubscriptionsByTopic operation.
type ListSubscriptionsByTopicBackend interface {
	ListSubscriptionsByTopic(ctx context.Context, in *ListSubscriptionsByTopicInput) (*ListSubscriptionsByTopicResponse, error)
}

// ListSubscriptionsByTopicHandler decodes a ListSubscriptionsByTopic request, dispatches
// to the backend, and encodes the response per awsQuery semantics.
// Form-encoded request bodies are decoded field-by-field for
// scalars, map<string,string>, and list<string> shapes. Complex
// collections (map<string,struct> like SNS MessageAttributes) are
// available to the backend via awsquery.FormFromContext(ctx).
func ListSubscriptionsByTopicHandler(b ListSubscriptionsByTopicBackend) http.Handler {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := awsquery.WithForm(r.Context(), r.Form)
		in := &ListSubscriptionsByTopicInput{}
		_ = in
		// Decode top-level scalar + map<string,string> + list<string>
		// form fields. AWS awsQuery serialises these as flat
		// name=value pairs with `.entry.N.{key,value}` (maps) and
		// `.member.N` (lists) conventions.
		if v := r.Form.Get("NextToken"); v != "" {
			s := v
			in.NextToken = &s
		}
		in.TopicArn = r.Form.Get("TopicArn")

		out, err := b.ListSubscriptionsByTopic(ctx, in)
		if err != nil {
			awsquery.WriteBackendError(w, err)
			return
		}
		awsquery.WriteResult(w, "ListSubscriptionsByTopic", out)
	})
}

// PublishBackend serves the Publish operation.
type PublishBackend interface {
	Publish(ctx context.Context, in *PublishInput) (*PublishResponse, error)
}

// PublishHandler decodes a Publish request, dispatches
// to the backend, and encodes the response per awsQuery semantics.
// Form-encoded request bodies are decoded field-by-field for
// scalars, map<string,string>, and list<string> shapes. Complex
// collections (map<string,struct> like SNS MessageAttributes) are
// available to the backend via awsquery.FormFromContext(ctx).
func PublishHandler(b PublishBackend) http.Handler {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := awsquery.WithForm(r.Context(), r.Form)
		in := &PublishInput{}
		_ = in
		// Decode top-level scalar + map<string,string> + list<string>
		// form fields. AWS awsQuery serialises these as flat
		// name=value pairs with `.entry.N.{key,value}` (maps) and
		// `.member.N` (lists) conventions.
		in.Message = r.Form.Get("Message")
		if v := r.Form.Get("MessageDeduplicationId"); v != "" {
			s := v
			in.MessageDeduplicationId = &s
		}
		if v := r.Form.Get("MessageGroupId"); v != "" {
			s := v
			in.MessageGroupId = &s
		}
		if v := r.Form.Get("MessageStructure"); v != "" {
			s := v
			in.MessageStructure = &s
		}
		if v := r.Form.Get("PhoneNumber"); v != "" {
			s := v
			in.PhoneNumber = &s
		}
		if v := r.Form.Get("Subject"); v != "" {
			s := v
			in.Subject = &s
		}
		if v := r.Form.Get("TargetArn"); v != "" {
			s := v
			in.TargetArn = &s
		}
		if v := r.Form.Get("TopicArn"); v != "" {
			s := v
			in.TopicArn = &s
		}

		out, err := b.Publish(ctx, in)
		if err != nil {
			awsquery.WriteBackendError(w, err)
			return
		}
		awsquery.WriteResult(w, "Publish", out)
	})
}

// GetTopicAttributesBackend serves the GetTopicAttributes operation.
type GetTopicAttributesBackend interface {
	GetTopicAttributes(ctx context.Context, in *GetTopicAttributesInput) (*GetTopicAttributesResponse, error)
}

// GetTopicAttributesHandler decodes a GetTopicAttributes request, dispatches
// to the backend, and encodes the response per awsQuery semantics.
// Form-encoded request bodies are decoded field-by-field for
// scalars, map<string,string>, and list<string> shapes. Complex
// collections (map<string,struct> like SNS MessageAttributes) are
// available to the backend via awsquery.FormFromContext(ctx).
func GetTopicAttributesHandler(b GetTopicAttributesBackend) http.Handler {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := awsquery.WithForm(r.Context(), r.Form)
		in := &GetTopicAttributesInput{}
		_ = in
		// Decode top-level scalar + map<string,string> + list<string>
		// form fields. AWS awsQuery serialises these as flat
		// name=value pairs with `.entry.N.{key,value}` (maps) and
		// `.member.N` (lists) conventions.
		in.TopicArn = r.Form.Get("TopicArn")

		out, err := b.GetTopicAttributes(ctx, in)
		if err != nil {
			awsquery.WriteBackendError(w, err)
			return
		}
		awsquery.WriteResult(w, "GetTopicAttributes", out)
	})
}

// SetTopicAttributesBackend serves the SetTopicAttributes operation.
type SetTopicAttributesBackend interface {
	SetTopicAttributes(ctx context.Context, in *SetTopicAttributesInput) (struct{}, error)
}

// SetTopicAttributesHandler decodes a SetTopicAttributes request, dispatches
// to the backend, and encodes the response per awsQuery semantics.
// Form-encoded request bodies are decoded field-by-field for
// scalars, map<string,string>, and list<string> shapes. Complex
// collections (map<string,struct> like SNS MessageAttributes) are
// available to the backend via awsquery.FormFromContext(ctx).
func SetTopicAttributesHandler(b SetTopicAttributesBackend) http.Handler {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := awsquery.WithForm(r.Context(), r.Form)
		in := &SetTopicAttributesInput{}
		_ = in
		// Decode top-level scalar + map<string,string> + list<string>
		// form fields. AWS awsQuery serialises these as flat
		// name=value pairs with `.entry.N.{key,value}` (maps) and
		// `.member.N` (lists) conventions.
		in.AttributeName = r.Form.Get("AttributeName")
		if v := r.Form.Get("AttributeValue"); v != "" {
			s := v
			in.AttributeValue = &s
		}
		in.TopicArn = r.Form.Get("TopicArn")

		if _, err := b.SetTopicAttributes(ctx, in); err != nil {
			awsquery.WriteBackendError(w, err)
			return
		}
		awsquery.WriteResult(w, "SetTopicAttributes", nil)
	})
}

// ListTagsForResourceBackend serves the ListTagsForResource operation.
type ListTagsForResourceBackend interface {
	ListTagsForResource(ctx context.Context, in *ListTagsForResourceRequest) (*ListTagsForResourceResponse, error)
}

// ListTagsForResourceHandler decodes a ListTagsForResource request, dispatches
// to the backend, and encodes the response per awsQuery semantics.
// Form-encoded request bodies are decoded field-by-field for
// scalars, map<string,string>, and list<string> shapes. Complex
// collections (map<string,struct> like SNS MessageAttributes) are
// available to the backend via awsquery.FormFromContext(ctx).
func ListTagsForResourceHandler(b ListTagsForResourceBackend) http.Handler {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		ctx := awsquery.WithForm(r.Context(), r.Form)
		in := &ListTagsForResourceRequest{}
		_ = in
		// Decode top-level scalar + map<string,string> + list<string>
		// form fields. AWS awsQuery serialises these as flat
		// name=value pairs with `.entry.N.{key,value}` (maps) and
		// `.member.N` (lists) conventions.
		in.ResourceArn = r.Form.Get("ResourceArn")

		out, err := b.ListTagsForResource(ctx, in)
		if err != nil {
			awsquery.WriteBackendError(w, err)
			return
		}
		awsquery.WriteResult(w, "ListTagsForResource", out)
	})
}
